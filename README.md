### Increase font size
```bash
sudo dpkg-reconfigure console-setup
```

### Change keyboard layout
```bash
sudo raspi-config
```
Then change localisation options

### SSH
```bash
sudo raspi-config
```
Network Options
